{
    "installCommand":"npm install --legacy-peer-deps",
    "version":2,
    "builds":[
        {
            "src":"src/index.js",
            "use":"@vercel/static-build"
        },
        {
            "src":"src/**/*",
            "use":"@vercel/static-build"
        }
    ],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"/"
        }
    ]
}